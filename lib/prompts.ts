import { TYPE_LABELS } from './riasec'
import type { RIASECScores } from '@/types'

// v2 — переключились на Claude API, расширенный анализ
export const PROMPT_VERSION = 'v2'

export function buildAnalysisPrompt(
  name: string,
  age: number,
  scores: RIASECScores,
): string {
  const sorted = (Object.entries(scores) as [keyof RIASECScores, number][])
    .sort(([, a], [, b]) => b - a)

  const scoresText = sorted
    .map(([t, s]) => `  ${TYPE_LABELS[t]}: ${s}/12 баллов`)
    .join('\n')

  const top3 = sorted.slice(0, 3).map(([t]) => TYPE_LABELS[t])
  const lowest = sorted.slice(-2).map(([t]) => TYPE_LABELS[t])

  return `Ты эксперт по профориентации для подростков в России. Твоя задача — дать глубокий, персональный и реально полезный анализ подростку, а не шаблонные советы.

ДАННЫЕ:
Имя: ${name}, возраст: ${age} лет
Результаты теста RIASEC:
${scoresText}

Топ-3 типа личности: ${top3.join(', ')}
Наименее выраженные типы: ${lowest.join(', ')}

ВАЖНЫЕ ПРАВИЛА:
1. Обращайся к ${name} напрямую, на «ты», тепло и с уважением
2. Пиши живо — как умный старший друг, а не учебник
3. Каждый раздел должен давать конкретную пользу, не воду
4. Анализируй КОМБИНАЦИЮ типов, не каждый по отдельности — именно сочетание даёт уникальный профиль
5. Рекомендуй только реальные профессии с хорошими перспективами в России и мире

ФОРМАТ ОТВЕТА — строго JSON:
{
  "personalityProfile": "2-3 предложения: кто такой ${name} исходя из комбинации его топ-типов. Не пересказывай баллы — дай образное, цепкое описание личности. Пример: 'Ты из тех редких людей, у кого аналитический ум сочетается с творческим взглядом — это делает тебя человеком, который видит проблему там, где другие видят просто задачу'",

  "motivationKey": "1 предложение: в чём твой главный внутренний двигатель? Что тебя реально заряжает — открытия, создание, помощь, управление, порядок? Будь точен и конкретен",

  "strengths": [
    "Конкретная сильная сторона 1 (не 'умный', а 'умеешь разбирать сложное на части и находить нестандартные решения')",
    "Конкретная сильная сторона 2",
    "Конкретная сильная сторона 3",
    "Конкретная сильная сторона 4"
  ],

  "growthAreas": [
    "Зона роста 1 — конкретно что развить и зачем это поможет в карьере",
    "Зона роста 2"
  ],

  "careerPath": "2-3 предложения: как может выглядеть путь ${name} через 5-10 лет, исходя из его профиля. Не пугай — вдохновляй. Покажи конкретную траекторию",

  "recommendations": [
    {
      "professionId": "slug из списка: programmer, data-scientist, doctor, scientist, cybersecurity, designer, filmmaker, journalist, game-developer, photographer, psychologist, teacher, social-worker, hr, entrepreneur, marketer, lawyer, project-manager, accountant, logistician, chef, engineer, pilot, architect, surgeon",
      "title": "Название профессии на русском",
      "matchPercent": число от 72 до 97,
      "explanation": "3-4 предложения: ПОЧЕМУ именно эта профессия подходит ${name}? Ссылайся на конкретные баллы и типы. Объясни связь между его личностью и реальными задачами этой профессии",
      "whyItFits": "1-2 предложения: самое главное совпадение — что в этой профессии будет давать ${name} максимальный кайф каждый день",
      "dayInLife": "2-3 предложения: конкретный день из жизни этого специалиста — живо и реалистично",
      "firstSteps": [
        "Первый шаг прямо сейчас (что можно сделать на этой неделе)",
        "Что изучить в ближайшие месяцы",
        "Куда двигаться дальше (кружок, олимпиада, курс, вуз)"
      ],
      "subjects": ["Предмет 1 для ЕГЭ/ОГЭ", "Предмет 2", "Предмет 3"],
      "personalNote": "Одно тёплое, личное, мотивирующее предложение к ${name} — что-то что хочется перечитать"
    }
  ]
}

Дай ровно 4 рекомендации. Выбирай профессии, которые реально соответствуют комбинации топ-3 типов ${name}.
Избегай очевидных клише — если у человека высокий investigative, не надо писать просто "учёный", найди более точное попадание.
Ответ ТОЛЬКО в JSON, без текста до или после.`
}
